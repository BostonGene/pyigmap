- name: test_main_rnaseq_sample_id
  command: ./pyigmap --mode rnaseq --sample_id SRR3743469 --reads_to_process 10000 --outdir ./ -profile docker
  files:
    - path: "pyigmap.tar.gz"
- name: test_main_amplicon_sample_id
  command: ./pyigmap --mode amplicon --sample_id SRR3743469 --reads_to_process 10000 --outdir ./ -profile docker
  files:
    - path: "pyigmap.tar.gz"